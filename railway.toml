[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn web_api:app --bind 0.0.0.0:${PORT:-8080} --workers 2"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
