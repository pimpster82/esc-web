[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn web_api:app --bind 0.0.0.0:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
