[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn web_api:app -c gunicorn_config.py"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
